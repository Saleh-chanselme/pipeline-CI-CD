# Pipeline CI/CD

This project is a ML pipeline CI CD using the **Iris dataset**.  
It includes:

- A **LogisticRegression** model trained using **scikit-learn**.
- Model saved and tracked using **MLflow**.
- A FastAPI backend to serve predictions.
- minimal Streamlit frontend for testing.

Use the menu on the left to explore the API and model documentation.
